services:
  social-fe:
    container_name: social-fe
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=https://api.th-red.app
    ports:
      - "3000:3000"
    restart: always